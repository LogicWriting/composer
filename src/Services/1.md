```
5.登录，正则校验，接口安全,防翻墙拦
接口安全是跨域 laravel自带跨域
防翻墙是JWT
短信/密码/静默
Sendsms 获取手机号并验证 发送验证码
手机号正则 /^1[3|5|7|8|9]\d{9}$/
发送验证码前判断验证码是否发送过，是否符合发送要求
如手机号格式，是否在规定时间内频繁发送，当天发送次数限制
二 表达获取或输入事件获取用户名/手机号 密码等 wx.requset调用接口
判断是否存在该用户，如果存在则验证密码，密码加密方式自定义，
若不存在则给出相应提示，
三 静默登陆 使用微信小程序的code(验证)请求微信服务器获取当前用户的
session_key和openid 判断errcode是否存在 存在则出现问题
若正常去mysql查询openid对应的用户是否存在，如果存在则说明登陆
成功，更新登陆时间和session_key 如果不存在则直接进行注册，创建
新用户。
```
```
轮播图
后台载入请求数据接口，获取到后台的指定图片
只取imageUrl 和 ID 即可
swiper标签 block标签循环渲染
内用swiper-item 然后写《image》正常渲染
跳转 navigator 利用属性url 跳转至小程序指定页面
富文本 rich-text 小程序自带的富文本标签

rich-text nodes
nodes属性内导入指定内容 可以解析HTML的dos
```
```
7.搜索,es检索,数据初始化,中文分词,高亮
根据版本不同有不同的方式 两种初始化索引方式不同 不然会报错
比如v7.15.2与v6.*版本
ik-worpk-max最大粒度分词
安装ik分词 将github上的ik分词解压缩后
移动至ES安装的plugins文件夹内
初始化索引时指定分词字段，这样在查询文档的时候就可以启用ik分词
```

```
8.工厂模式封装,调用
创建一个接口类作为产品原型
内含相应的原型行为（方法）
二创建两个继承原型的可用产品
重写原本的原型方法，建立可用的产品
建立一家角色基类，再建立两家继承原型角色的不同可用角色
不同角色销售不同的可用产品 也就是不同实例
建立门店（工厂实现类）
定义销售的方法，内部判断条件进行不同的产品销售，也就是用不同的
角色去进行不同的产品实例去给进行销售
```
```
9.地图定位
前台小程序使用wx.ChooseLocation方法进行选择当前位置
切记小程序版本要切换到2.19.4左右 否则会报错或者无响应
```
```
10.文件上传,云存储,图片压缩,防盗链,访问优化
使用李硕老师的composer包，
将文件对象直接写入put的参数2 参数1是自定义文件名
然后在OSS云存储进行图像处理如缩略图/水印/还有上传加速等等
```
```
11.Swoole,一对一聊天,主动断开链接
首先自定义方法，onLoad时调用
内含wx.connect(‘ws://110.40.207.218:9502’)
链接成功后回调success 写上wx.onSocketOpen监听服务器状态
自定义数据发送给socket服务器 切记使用json格式
小程序使用JSON.stringify转json 自定义发送服务器方法
内含 谁连接了 （用户名）和 当前消息的状态
使用wx.SendSocketMessage 传递参数使用data ： msg或者params
告诉服务器是谁连接了 同时将数据（fd）存储到redis
同时在客户端开启服务器消息监听 也就是onSocketMessage
他可以时刻监听和接收服务器返回的数据 在服务器也要自定义数据
使用ws->push($frame->fb,json_encode（数据）)
内含你发送给谁 谁发送的 发送的内容和发送的状态。
服务器解析使用json_decode就可以解json为数组进行使用
然后就是渲染聊天记录
一种偏门就是客户端使用变量存储发送记录和接收记录，将自己和他人发送的记录统一的进行合并处理，然后渲染页面，因为包含用户名和内容，
所以可以直接进行渲染。
第二种才是正确的 要在服务器进行foreach 这种还没有搞懂
```
```
12.定时任务,数据统计,更新
首先使用php artisan make:command创建定时任务类
内部声明signature = ‘类名称 如’
TestCon 定义为 test:con 然后在handle方法内写入要进行的行为操作
完成上一步后在Kermel中注册，自带的注释解开后在command中
写上之前定义的类名称 然后-》规定的执行时间即可
然后使用php artisan 查看是否注册成功
使用 php artisan 类名称开启常驻进程
如 php artisan test:con
```
```
13.缓存应用
缓存数据优化页面等等
new \Redis（） 原生使用redis-connect(127.0.0.1,6379)
即可连接本地 可以使用redis-set(key,json_encode（data）)
字符串存储数据
```


